# Boltz-2 Integration

Boltz is executed through CLI (`boltz predict`) from the REINVENT wrappers.
No patched Boltz source code is required in this repository.

If you need local Boltz source for development, clone it into `../boltz` (gitignored placeholder at repo root).
