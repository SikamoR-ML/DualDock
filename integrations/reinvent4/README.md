# REINVENT4 Integration

DualDock uses REINVENT4 `ExternalProcess` scoring components.
The wrappers in `external_process/` are the runtime bridge between generated SMILES and Boltz scoring.

Active modules:
- `boltz_single_wrapper.py`
- `boltz_dual_wrapper.py`
